[phases.setup]
nixPkgs = ["nodejs_18", "chromium", "nss", "freetype", "harfbuzz", "ca-certificates"]

[phases.install]
cmds = ["npm ci --only=production --no-optional"]

[phases.build]
cmds = ["echo 'Build completed'"]

[start]
cmd = "npm start"
